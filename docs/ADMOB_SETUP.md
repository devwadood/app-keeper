# AdMob setup

Enable the AdMob API and ensure the connected identity can access the intended publisher.
Connect the AdMob product scope, select discovered publishers, and run a report test.
Confirm publisher currency and timezone. Daily reports group by DATE and APP. Estimated
earnings are labelled preliminary because Google may revise them.
